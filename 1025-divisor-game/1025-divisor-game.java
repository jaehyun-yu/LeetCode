class Solution {
    public boolean divisorGame(int n) {
        boolean answer = false;
        
        if (n % 2 == 0) {
            answer = true;
        }
        
        return answer;
    }
}